
function reverseInPlace(array) {
let l = array.length;

  for (let i = 0; i < l; i += 2) {
     array.unshift(array[i]);
   }
   array.splice(Math.floor(l / 2), l / 2);
   return array;
}
console.log(reverseInPlace([1,2,3,4,5]));
